query {
  # If non-existing id null is returned
  user(id: "23") {
    id
    firstName
    age
  }
}

query {
  user(id: "23") {
    id
    firstName
    age
    company {
      id
      name
      description
    }
  }
}

query {
  user(id: "41") {
    id
    firstName
    age
    company {
      id
      name
      description
    }
  }
}

query {
  company(id: "1") {
    id
    name
    description
  }
}

query {
  company(id: "1") {
    id
    name
    description
    users {
      id
      firstName
      age
    }
  }
}

# Strange nested circular relations
query {
  company(id: "1") {
    id
    name
    description
    users {
      id
      firstName
      age
      company {
        id
        name
        description
      }
    }
  }
}
