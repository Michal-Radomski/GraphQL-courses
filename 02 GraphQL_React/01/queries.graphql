query {
  # If non-existing id null is returned
  user(id: "23") {
    id
    firstName
    age
  }
}

query {
  user(id: "23") {
    id
    firstName
    age
    company {
      id
      name
      description
    }
  }
}
